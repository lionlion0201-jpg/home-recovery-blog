# pending

**承認待ちのマニフェスト置き場。**

`run-promotion.yml` は `docs/cycles/cycle_manifest_*.json` のpushで発火する。
このフォルダに置いてある間はそのパス指定に一致しないため、**X投稿は走らない。**

週次パイプラインは生成したマニフェストをここに置く。
`scripts/apply_review.py` が承認された記事のマニフェストだけを
`docs/cycles/` へ移し、そのpushで初めて投稿処理が動く。

2026-09-21以前は、生成したマニフェストを直接 `docs/cycles/` に置いていたため、
人間の承認を経ずにX投稿と記事公開が行われていた。共通ルール文書に
「最終的な公開承認は必ず人間が行う」と書いてあるにもかかわらず、である。
この仕組みはその是正。
